"""Configuration loading and validation.

This module should define typed configuration objects for model adapters,
variable spaces, optimizers, objectives, metrics, artifacts, and full runs.
It should load YAML or dictionaries, apply defaults, validate incompatible
choices, and expose normalized values to the rest of the package.
"""

