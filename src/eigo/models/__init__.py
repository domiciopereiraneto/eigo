"""Diffusion model adapter package.

Model adapters provide a common interface around different text-to-image
pipelines while isolating model-specific tokenization, conditioning, latent
shape, generation, dtype, scheduler, and device behavior.
"""

