"""Base interface for diffusion model adapters.

This module should define the abstract contract that every model adapter must
implement: loading resources, preparing prompt conditioning, initializing
model-aware variable metadata, converting variable-space outputs into pipeline
inputs, and generating images.
"""

