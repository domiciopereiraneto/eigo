"""Base interface for optimization variable spaces.

This module should define how a variable space initializes candidates, exposes
shape and bounds, projects raw optimizer candidates into valid variables, and
converts variables into model-adapter inputs.
"""

